# micro_ids
run ./project <no> <password>

iperf.py:
TCP mode, a server in fpga and sequence client call among each node.

iperf_t5.py:
TCP mode, server in each node and sequence client call among each node.

iperf_r.py
command:
./project <no> <password> <protocal> <bitfile>

default TCP NIC

for step_6
./project <no> <password> udp nic
./project <no> <password> udp router

